[package]
name = "simple-crate"
version = "1.0.0"
edition = "2021"
rust-version = "1.56"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
tempfile = "3.10"

[build-dependencies]
cc = "1.0"
