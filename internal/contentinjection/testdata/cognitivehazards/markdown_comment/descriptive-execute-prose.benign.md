# Build system documentation

<!-- The build system will automatically execute the test suite whenever a file in src/ or tests/ is modified by the developer's editor. -->

The above documentation comment describes legitimate behavior. It
contains the catalog verb "execute" but mid-sentence, with a
subject ("the build system"), describing what something WILL do
rather than directing the reader/agent to do something. Only one
catalog verb total, not at first position. The detector must not
fire.

## Section

Ordinary prose continues.
